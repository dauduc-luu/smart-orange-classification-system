# Models

Place YOLO weights in this folder, for example:
- models/yolov8n.pt
- models/yolov8s.pt

If you use Ultralytics, the model can be downloaded automatically on first run. For this project, the main laptop script expects the model file under the models/ directory.
